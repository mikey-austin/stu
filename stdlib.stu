;;
;; Stdlib functions and definitions.
;;

;; Our nil symbol.
(def nil ())

;; nil predicate.
(def nilp (lambda (a) (= a nil)))

(def map (lambda (f l)
           (if (nilp l)
               nil
             (cons (f (car l)) (map f (cdr l))))))
